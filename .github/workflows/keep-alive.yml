name: Keep Services Alive

on:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Health Endpoint
        run: |
          echo "Pinging backend..."
          curl -s ${{ secrets.BACKEND_URL }}/health